import 'package:flutter/widgets.dart';

class PendingFriendTile extends StatelessWidget {
  const PendingFriendTile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}